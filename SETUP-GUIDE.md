# 雲端同步/備份設定指南（你自己專用，僅需設定一次）

隨手記帳預設把所有紀錄存在手機本機（離線可用），但如果想要「換手機時不遺失資料」或「有個雲端備份」，可以連到你自己的 Google Sheet 當作免費、免維運的同步後端。這個功能是選用的——不設定的話，App 一樣正常運作，只是資料只留在這台裝置上。

這不是「即時多裝置同步」（例如手機打的帳，平板不會馬上自動出現），而是「推送備份／拉取還原」：你按「立即同步」時才會跟 Google Sheet 交換資料，換新手機時按「從雲端完整還原」把資料抓回來。

## 建立你的 Google Sheet

1. 到 [Google 試算表](https://sheets.google.com) 新增一個空白試算表，取個名字（例如「隨手記帳資料」）。
2. 不需要手動填欄位——第一次同步時，`Code.gs` 會自動幫你補上表頭欄位。

## 部署步驟

1. 開啟你剛建立的 Google Sheet。
2. 點選選單「**擴充功能（Extensions）→ Apps Script**」，會開啟一個新分頁的程式碼編輯器。
3. 把編輯器裡預設的 `function myFunction() {}` 全部刪除，貼上這個專案 `Code.gs` 檔案的完整內容，然後按左上角儲存（磁片圖示）。
4. 點右上角藍色「**部署（Deploy）→ 新增部署作業（New deployment）**」。
5. 左邊齒輪圖示選「**網頁應用程式（Web app）**」。
6. 設定：
   - 說明：可填「隨手記帳同步」
   - 執行身分（Execute as）：**我（你的帳號）**
   - 誰可以存取（Who has access）：**任何人（Anyone）** ← 這一步一定要選「任何人」，否則手機連不到
7. 點「部署」。第一次部署會跳出 Google 的授權確認畫面（因為腳本要讀寫你的 Sheet）：
   - 選你的帳號 → 若出現「Google 尚未驗證這個應用程式」，點左下角「進階」→「前往 (不安全)」→ 允許。這是正常現象（因為這是你自己寫的私人腳本，沒有送 Google 審查），不是真的有安全疑慮。
8. 部署完成後會顯示一組網址，格式類似：
   ```
   https://script.google.com/macros/s/AKfycb.................../exec
   ```
   複製這組網址。

## 把網址接上 App

打開隨手記帳，到「設定」頁籤，把剛剛複製的網址貼到「Apps Script 部署網址」欄位，按「儲存網址」。

**這組網址等同存取金鑰**——部署設定是「任何人皆可存取」，代表知道這個網址的人理論上都能讀寫你的記帳資料。對個人記帳用途通常可以接受，但請不要把這組網址分享給不信任的人。

## 驗證部署是否成功

部署完成的網址直接貼到瀏覽器網址列開啟（GET 請求），應該會看到：

```json
{"ok":true,"message":"隨手記帳同步伺服器運作中。請用 POST 傳送 JSON body，例如 {\"action\":\"pull\",\"since\":0}"}
```

看到這個就代表部署成功。**注意：不要用 `curl` 測試實際的推送/拉取（POST 請求）**，Apps Script 的轉址機制會讓 curl 出現誤導性錯誤（不代表真的壞了）；請直接在 App 裡按「立即同步」測試。

## 換新手機時如何拿回資料

1. 在新手機上把 App 加入主畫面、開啟。
2. 到「設定」頁籤，貼上同一組 Apps Script 部署網址，按「儲存網址」。
3. 按「從雲端完整還原（新裝置用）」，會把 Google Sheet 裡的所有紀錄拉回這台裝置。

## 修改過 Code.gs 之後

Apps Script 修改程式碼後，必須到「部署 → 管理部署作業 → 編輯（鉛筆圖示）→ 版本選「新版本」→ 部署」才會生效，只存檔不會自動更新已部署的網址（部署網址本身不會變，不需要重新貼到 App 裡）。

## 常見問題

- **App 顯示「尚未設定同步網址」**：到「設定」頁籤確認網址欄位有填、有按過「儲存網址」。
- **同步按下去顯示「同步失敗」**：先確認步驟 6 的存取權限是否真的選了「任何人」；再確認網址有沒有貼對（結尾應該是 `/exec`）。
- **Google Sheet 裡看到 `deleted` 欄位是 TRUE 的列，可以刪掉嗎？**：不建議手動刪除——`deleted` 是軟刪除標記，用來讓其他裝置在下次「拉取還原」時也知道要刪除這筆紀錄。如果手動把整列刪掉，其他還沒同步過這筆刪除的裝置會誤以為這筆紀錄還存在。
- **想徹底清空雲端備份重新開始**：直接把 Google Sheet 裡表頭以外的所有列刪除即可，App 端不受影響（本機資料還在，下次同步會重新把本機所有紀錄推上去）。
